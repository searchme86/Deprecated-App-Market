import styled from 'styled-components';

export const SpinnerWrapper = styled.div`
  position: fixed;
  top: 50%;
  left: 50%;
  transform: translateO(-50%, -50%);
`;

export const SpinnerContainer = styled.div``;
